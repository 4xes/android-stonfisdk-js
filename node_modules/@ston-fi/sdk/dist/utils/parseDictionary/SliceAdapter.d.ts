import type { Slice } from "tonweb";
import type { Cell } from '../../types';
export declare class SliceAdapter {
    slice: Slice;
    refs: Cell[];
    constructor(cell: Cell);
    loadBit(): boolean;
    loadUint(bits: number): number;
    loadUintBN(bits: number): import("bn.js");
    loadRef(): Cell;
}
export declare function beginParse(cell: Cell): SliceAdapter;
